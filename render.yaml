services:
  - type: web
    name: elevyr-api
    runtime: python
    region: frankfurt
    plan: free
    rootDir: apps/api
    buildCommand: pip install -e .
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite+aiosqlite:///./elevyr.db
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        value: https://elevyr.vercel.app
      - key: DEBUG
        value: "false"
    healthCheckPath: /health
